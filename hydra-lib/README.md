# Hydra API library

Hydra API library, containing APIs to be used by the Hydra runtime and the benchmarks.

### Build

Run the `build.sh` script. This component is not deployable since it merely defines an API.
