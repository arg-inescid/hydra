package com.lambda_manager.collectors.meta_info.impl;

import com.lambda_manager.collectors.meta_info.Lambda;

@SuppressWarnings("unused")
public class DefaultLambda extends Lambda {

    public DefaultLambda(int id) {
        super(id);
    }
}
