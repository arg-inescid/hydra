package com.lambda_manager.collectors.lambda_info.impl;

import com.lambda_manager.collectors.lambda_info.LambdaInstanceInfo;

public class DefaultLambdaInstancesInfo extends LambdaInstanceInfo {
    public DefaultLambdaInstancesInfo(int id) {
        super(id);
    }
}
